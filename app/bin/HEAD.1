HEAD(1) 		UZIX Text Utilities		  HEAD(1)


NAME
       head - print the first few lines of a file

USAGE
       head [-n] [files...]

DESCRIPTION
       This manual documents the UZIX version  of head. It's been
       scaled down for inclusion in the UZIX release, and doesn't
       have the help or version  commands  included  in  the  GNU
       release. Also, many options are missing.

       head print first few lines  of  one  or  more  files.  The
       default count is 10 lines.  The default file is stdin.

OPTIONS
       -n     How many lines to print

AUTHORS
       Program :- Andy Tanenbaum
       Documentation :- Adriano Cunha <adrcunha@dcc.unicamp.br>

HEAD(1) 		UZIX Text Utilities		  HEAD(1)
